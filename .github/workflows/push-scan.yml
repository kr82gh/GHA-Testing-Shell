name: Push Scan

on: 
  push:

permissions:
 contents: write

jobs: 
  k8s-run:
    name: k8-run

    runs-on: ubuntu-latest

    steps:
    - name: checkout-stuff
      uses: actions/checkout@v7 #Must have the "v" for  version 

    - name: always-run
      if: always ()
      run: | 
        echo "logging out" 

    container: 
      image: bridgecrew/checkov


